const title = "E-commerce shop";
const description = "The best e-commerce shop";

const nextSeoConfig = {
  title,
  description,
  openGraph: {
    title,
    description,
    site_name: "Nasz sklep",
  },
};

export default nextSeoConfig;
